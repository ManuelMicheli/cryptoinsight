export default function CategoryTag({ category }) {
  return (
    <span className="px-3 py-1 rounded-full bg-neon-purple/10 text-neon-purple text-xs font-medium tracking-wide uppercase">
      {category}
    </span>
  )
}
